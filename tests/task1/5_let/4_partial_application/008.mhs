main :: Int
     =
 let x :: Int = 8;

 -- do they correctly get the environment for the partial eval?
 in let 
     f :: (Int -> Int -> Int) =
        letfun f :: (Int -> Int -> Int) w =
          letfun g :: (Int -> Int) y =
               y + x + w + 1;

 in f 3 2;
